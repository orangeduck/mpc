#include "stdio.h"

int fib(int n) {
  if (n == 0) { return 0; } 
  if (n == 1) { return 1; }
  return fib(n - 1) + fib(n - 2);
}

main() {
  
  int n;
  int i;
  
  while (i < 10) {
    n = fib(10);
    print(n);
    i = i + 1;
  }

  return 0;
}